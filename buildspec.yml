version: 0.1

phases:
  build:
    commands:
      - npm install npm -g
      - npm install
      - npm run dev
artifacts:
  files:
    - index.html
    - favicon.ico
    - MattHainesResume.pdf
    - styles/main.css
    - images/*
    - dist/main.js
